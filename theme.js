const baseTheme = require('@theme-ui/preset-base').default

module.exports = {
    ...baseTheme,
    layout: {
        header: {
            backgroundColor: 'red',
            padding: 3
        }
    }
}